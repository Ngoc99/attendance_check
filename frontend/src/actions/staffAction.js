import {
  STAFF_CREATE_REQUEST,
  STAFF_CREATE_SUCCESS,
  STAFF_CREATE_FAIL,
} from "../constants/staffConstant";

export const staffCreate = (sid) => async (dispatch) => {
  //   dispatch({ type: STAFF_CREATE_REQUEST });
};
